# Phụ thuộc cho bản deploy. Khác requirements.txt ở hai điểm:
#
# 1. torch bản CPU. Đo thật 07/09/2026: venv với torch CUDA 5,9 GB, với torch
#    CPU 1,6 GB. Container Apps không có GPU, và đo trên chính app này thì GPU
#    chỉ tiết kiệm 25 ms trên 2,3 giây (nút thắt là API Groq, chạy ở máy họ).
#
# 2. GHIM CHÍNH XÁC phiên bản, không dùng >=. Lý do không phải sạch sẽ mà là
#    đúng đắn:
#    - `chromadb` đã đổi định dạng đĩa qua các bản 0.4/0.5/1.0. Kho trên đĩa
#      được ghi bởi 1.5.9; một bản khác có thể không đọc được, và lỗi sẽ nổ
#      vào một buổi sáng không ai sửa code.
#    - 49.063 vector trong kho được tính bởi đúng bộ torch 2.13.0 +
#      sentence-transformers 6.0.1 + transformers 5.16.1. Nạp thêm văn bản
#      bằng bộ khác là trộn hai không gian nhúng trong cùng một chỉ mục HNSW:
#      chất lượng tụt âm thầm, không test nào bắt được.
#      Cách duy nhất phát hiện là evaluation/retrieval_eval.py.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.13.0+cpu
sentence-transformers==6.0.1
transformers==5.16.1
tokenizers==0.23.1
numpy==2.2.6

chromadb==1.5.9

fastapi==0.141.1
uvicorn==0.52.4
openai==1.109.1
pydantic==2.13.5
python-dotenv==1.2.3
requests==2.34.2

# KHÔNG có ở đây, và có lý do:
#   pymupdf      - chỉ crawler dùng, bản phục vụ không bóc PDF bao giờ
#   gradio       - tàn dư của đường HuggingFace Spaces đã bỏ
#   datasets     - chỉ dùng lúc nạp kho lần đầu
#   ragas        - chỉ dùng lúc đánh giá
#   pytest       - không chạy test trên bản deploy
